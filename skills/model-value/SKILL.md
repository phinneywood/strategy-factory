---
name: model-value
description: Assess a proposal's value, costs, opportunity costs, adoption and requested commitment.
---

# model-value

Read the installed `strategy-factory` instructions through the host's skill reader (or the bundled `../strategy-factory/SKILL.md`). Apply section 3 and its shared document and coaching rules to this request; do not run every stage.
If the core cannot be read, report that dependency as unavailable instead of inventing its method.
